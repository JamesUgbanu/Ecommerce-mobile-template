import React from 'react';

import TemplatePlaceholderScreen from '../../components/common/TemplatePlaceholderScreen';

const BagScreen = () => {
  return (
    <TemplatePlaceholderScreen
      title='Bag'
      description='Use this screen as the starting point for cart state, checkout, promotions, and order summaries.'
    />
  );
};

export default BagScreen;
