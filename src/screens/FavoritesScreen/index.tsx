import React from 'react';

import TemplatePlaceholderScreen from '../../components/common/TemplatePlaceholderScreen';

const FavoritesScreen = () => {
  return (
    <TemplatePlaceholderScreen
      title='Favorites'
      description='Save wishlists here or wire this tab to your backend favorites collection when the template grows into a production app.'
    />
  );
};

export default FavoritesScreen;
